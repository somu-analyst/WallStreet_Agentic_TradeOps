{
  "_comment": "Register the NYSE_DATA options engine as an MCP server. For Claude Desktop, merge this into claude_desktop_config.json (Windows: %APPDATA%/Claude/claude_desktop_config.json). Adjust the python path if you use a venv. The server imports telegram_bot_optimized as a library (read-only tools) and speaks JSON-RPC over stdio.",
  "mcpServers": {
    "nyse-options": {
      "command": "python",
      "args": ["C:/Users/srini/Options_chain_data/NYSE_DATA/mcp_server.py"]
    }
  }
}
